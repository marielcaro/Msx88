PIC	EQU	20H
EOI	EQU	20H  
NF10	EQU	10

	ORG	1000H
ELEGIDO	DB	?

	ORG 	40
IP_F10	DW	RUTF10

	ORG 	2000H
	CLI	
	MOV 	AL,0FEH	
	OUT	PIC+1,AL
	MOV	AL, NF10
	OUT	PIC+4, AL
	STI
	MOV 	DX,0
	MOV	CX,5
LAZO:	INC	DX
	DEC	CX
	JNZ 	LAZO
	MOV	DX,0
	MOV 	CX,5
	JMP 	LAZO

	ORG 	3000H
RUTF10:	MOV 	ELEGIDO, DX
	IRET

	END

	
